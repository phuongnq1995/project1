<div class="center jumbotron">
  <h1><%= t "activities" %></h1>
      <% if logged_in? %>
        <%= link_to "continue", categories_path, class: "btn btn-primary"%>
      <% else %>
        <h3>please_signin</h3>
      <% end %>
</div>
